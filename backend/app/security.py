# DEPRECATED: this file has been replaced by the ``app.security`` package
# (see ``app/security/__init__.py``).  Python's import system prefers the
# package directory over a same-name module, so this file is effectively
# unreachable.  Left in place because we cannot delete files in some
# build/deploy contexts; safe to remove on next cleanup.
